sudo: false
language: java
cache:
  directories:
  - "$HOME/.cache"
  - "$HOME/.m2"
install:
  provider: script
  script: mvn -DskipITs clean install
  
deploy:
  provider: script
  script: mvn -DskipITs sonar:sonar
